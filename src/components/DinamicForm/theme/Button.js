const MyButton = {
    variants: [
        {
            props: { color: "primary", variant: "contained" },
            style: {
                backgroundColor: 'red',
                fontSize: 20,
                minWidth: "200px"
            }
        },
    ]
}

export default MyButton